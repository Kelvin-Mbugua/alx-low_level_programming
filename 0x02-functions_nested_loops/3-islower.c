#include "main.h"

/**
 * _islower - Checks if a character is lowercase.
 *
 * Return: 1 if character is lowercase, 0 otherwise.
 */
int _islower(int c)
{
	int r;
	r = _islower(c);
	putchar(r + '0');
	putchar('\n');
}
